import 'package:flutter/material.dart';

// colors that we use in our app

const kMainColor = Color(0xFF000000);
const kActiveColor = Color(0xFF007100);
const kAccentColor = Color(0xFFFFA500);
const kBodyTextColor = Color(0xFF808080);
const kInputColor = Color(0xFFFFFFFF);
const kbgColor = Colors.white;

const double defaultPadding = 16.0;